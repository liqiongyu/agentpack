## 1. Spec (OpenSpec)
- [ ] Modify `openspec/specs/agentpack-cli/spec.md` to define patch overlay rebase behavior.

## 2. Docs (implementation contract)
- [ ] Update `docs/SPEC.md` overlay rebase section to include patch overlays.

## 3. Validation
- [ ] `openspec validate add-patch-overlay-rebase --strict --no-interactive`
